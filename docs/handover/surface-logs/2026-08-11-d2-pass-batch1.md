# D2 surface pass — batch 1 (2026-08-11)

**Target:** D2 Daily-ops Ready  
**Method:** Code connectivity + domain review (live NAS clerk click residual)  
**Branch work:** unit-list cap under filter, unit→WO deep link, lease list open UX, production demo wipe guard  

---

## A1 `/Account/Login` — PASS WITH NOTES

**Job story:** As clerk, I sign in with town email/password and land on the page I requested (or dashboard).

| Check | Result |
|-------|--------|
| Arrive | Route + EmptyLayout + Identity cookie SSR form |
| Format | SfCard + primary Sign in; Bootstrap inputs (acceptable for SSR Identity) |
| Connected | `SignInManager.PasswordSignInAsync`; safe `ReturnUrl` via `LoginRedirectHelper` (open-redirect hardened) |
| Usable | Required email/password; clear error text |

**Bugs:** none S0/S1  
**Residual:** Clerk smoke on NAS with real credentials  
**Notes:** S3 — not full Syncfusion inputs (correct for static SSR login)

## A2 Shell (MainLayout) — PASS WITH NOTES

**Job story:** As clerk, I navigate Daily work / Records / Account, toggle theme, open PayStar, recover from page errors.

| Check | Result |
|-------|--------|
| Arrive | SfSidebar + header + toast host + error boundary |
| Format | Fluent nav sections; theme switch; user email display |
| Connected | All nav routes exist; CC deep links when facility unit present; PayStar from options |
| Usable | Error boundary “Dismiss and retry”; mobile menu toggle |

**Bugs:** none S0/S1  
**Notes (S2/S3):** `CloseOnNavigate` collapses sidebar after every click (fine mobile; desktop Auto may re-expand). “CC documents” and “Documents” both → `/documents` (duplicate entry, intentional vault).  
**Residual:** Clerk visual pass light/dark on NAS

## C1 `/units` — PASS WITH NOTES (fix applied)

**Job story:** As clerk, I list units, filter vacant from dashboard, open a unit, add/edit within portfolio cap.

| Check | Result |
|-------|--------|
| Arrive | SfGrid inline edit + Open |
| Format | Empty template; Excel filter; delete confirm |
| Connected | `IUnitService` CRUD; `?status=` filter; facility delete blocked; occupied delete blocked |
| Usable | Open → `/units/{id}` |

**Bugs fixed this pass:**  
- **S1:** Residential cap counted **filtered** rows → could allow Add over MaxUnits when status filter active. Now uses `AllUnits`.  
- **S2:** Filter banner + Clear filter.

**Residual:** Clerk add/edit/delete smoke on NAS

## C2 `/units/{id}` — PASS WITH NOTES (fix applied)

**Job story:** As clerk, I manage unit inventory (assets/flooring), see occupancy, jump to tenant/WO/ops costs.

| Check | Result |
|-------|--------|
| Connected | Multiple services; tenant link; occupancy history grid |
| Fixed | Maintenance link → `maintenance?unitId=` (was bare `/maintenance`) |

**Residual:** Tab-by-tab clerk exercise

## C3 `/tenants` — PASS WITH NOTES

Open/double-click → detail; soft-delete; deposit-aware list service. No code change.

## C4 `/tenants/{id}` — PASS WITH NOTES

Links to unit, lease, payments (`tenantId`/`action`), receipt, PayStar. Nested household/ledger paths present. No code change this batch.

## D1 `/leases` — PASS WITH NOTES (fix applied)

New lease CTA; Open + **double-click**; empty-state copy fixed (“above” not “toolbar”).

## I1 `/settings` (early S0 guard) — PASS WITH NOTES (fix applied)

**S0 risk fixed:** Force reseed (wipe demo) **hidden + server-guarded in Production**.

---

## Progress snapshot

| ID | Verdict |
|----|---------|
| A1 | PASS WITH NOTES |
| A2 | PASS WITH NOTES |
| C1 | PASS WITH NOTES |
| C2 | PASS WITH NOTES |
| C3 | PASS WITH NOTES |
| C4 | PASS WITH NOTES |
| D1 | PASS WITH NOTES |
| I1 | PASS WITH NOTES (partial; full settings session still open) |
| D2–H* / F* / G* / B1 | Not yet formally logged |

**Next surface:** **D2** `/leases/new` (wizard job story), then **D3** lease detail, **E1** payments.

## D2 `/leases/new` — PASS WITH NOTES (fix applied)

**Job story:** Create & generate lease from template + unit + tenant → land on lease detail.

| Check | Result |
|-------|--------|
| Connected | Templates from DocumentRoot; CreateDraft + GenerateDocuments; navigate to detail |
| Empty templates | Clear alert + TEMPLATES.md pointer |
| Fixed | Unit dropdown excludes facility (CC) units |

**Residual:** Clerk run with real Brookside templates on NAS.

## D3 `/leases/{id}` — PASS WITH NOTES

Lifecycle Amend/Renew/Terminate panels; signed PDF upload; null e-sign messaging; prior/successor links; PDF viewer path. No S0/S1 found in code review.

**Residual:** Clerk full lifecycle on one demo lease.
